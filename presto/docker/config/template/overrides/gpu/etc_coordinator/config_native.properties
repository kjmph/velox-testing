# Infer NOT NULL columns from function metadata when planning GPU joins.
optimizer.joins-not-null-inference-strategy=USE_FUNCTION_METADATA
# Use a default filter factor when statistics do not provide selectivity.
optimizer.default-filter-factor-enabled=true
cluster-tag=native-gpu
